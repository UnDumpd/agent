# Normalize all text files to LF in the repo; check out LF on every platform.
# Prevents CRLF churn when editing on Windows/WSL (see review 2026-08-03).
* text=auto eol=lf

*.dump binary
testdata/*.sql binary
*.sh text eol=lf
